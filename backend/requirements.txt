# --- Core framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Settings / env ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
email-validator==2.2.0

# --- Database (async SQLAlchemy + Postgres) ---
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.3

# --- Auth / security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # pinned: passlib 1.7.4's backend self-test breaks on bcrypt>=4.1 (ValueError: password cannot be longer than 72 bytes)
python-multipart==0.0.9

# --- Background jobs (audit runs execute async, off the request thread) ---
celery==5.4.0
redis==5.0.8

# --- HTTP client for the crawler / external checks (PageSpeed, etc.) ---
httpx==0.27.2

# --- Crawler (crawler/): HTML parsing for parser.py/links.py/extractor.py ---
beautifulsoup4==4.12.3
lxml==5.3.0

# --- Crawler (crawler/screenshots.py, consent/screenshots.py) — needed for
# screenshot capture (full-page + banner-clip). Also requires the one-time
# `playwright install chromium` run in the Dockerfile below.
playwright==1.47.0

# --- PDF export (pdf/) — renders reports/generator.py's ReportPayload to a
# vector PDF (cover, charts, tables) without a browser/HTML engine. ---
reportlab==4.4.10

# --- Logging ---
loguru==0.7.2

# --- CORS is bundled with FastAPI/Starlette, no extra package needed ---
