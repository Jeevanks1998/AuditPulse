/* ==========================================================================
   env.js — deployment-specific backend URL.
   Local dev: leave as-is (falls through to config.js's localhost default).
   Vercel: this file is REGENERATED at build time by scripts/generate-env.sh
   using the BACKEND_API_URL environment variable set in the Vercel project.
   ========================================================================== */
window.__AUDITPULSE_API_BASE__ = window.__AUDITPULSE_API_BASE__ || '';
